eureka:
  client:
    enabled: false

spring:
  application:
    name: account-service
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/account-test-db

kafka:
  bootstrap-servers: localhost:29092
  consumer:
    group-id: account.consumer.test

topics:
  bank-account-created: bank.account.created
  bank-debitcard-created: bank.debitcard.created
  bank-account-activated: bank.account.activated

clients:
  customer-service:
    base-url: http://localhost:8081
