spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/account-db}
      auto-index-creation: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
