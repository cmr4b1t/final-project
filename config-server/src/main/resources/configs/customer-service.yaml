spring:
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/customer-db}

kafka:
  bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
  consumer:
    group-id: ${KAFKA_CONSUMER_GROUP_ID:customer.consumer}

topics:
  bank-account-created: bank.account.created
  bank-debitcard-created: bank.debitcard.created
  bank-account-activated: bank.account.activated

management:
  endpoints:
    web:
      exposure:
        include: health,info