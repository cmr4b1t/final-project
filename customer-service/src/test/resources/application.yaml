eureka:
  client:
    enabled: false

spring:
  application:
    name: customer-service
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/customer-test-db

kafka:
  bootstrap-servers: localhost:29092
  consumer:
    group-id: customer.consumer.test

topics:
  bank-account-activated: bank.account.activated

